export const DATA_TABLE_CONFIG = {
  textOperators: [{ label: 'Contains', value: 'iLike' as const }],
  numericOperators: [{ label: 'Is', value: 'eq' as const }],
  selectOperators: [{ label: 'Is', value: 'eq' as const }],
  booleanOperators: [{ label: 'Is', value: 'eq' as const }]
}
